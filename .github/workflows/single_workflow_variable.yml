name: Hello world
on: workflow_dispatch
jobs:
  multiple_shell_commands:
    runs-on: ubuntu-latest
  steps:
    - name: Run multiple commands
      run: |
        echo "Hello world!"
        echo "This is a multiple command step."

