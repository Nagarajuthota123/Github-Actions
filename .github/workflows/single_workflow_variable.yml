name: demo of environment variables
on: workflow_dispatch
env: 
  APP_ENV: workflow-level
jobs:
  env-variable-demo:
    env: 
      APP_ENV: job-level
    runs-on: ubuntu-latest
    steps:
      - name: knowing the env variable defining on stage
        run: echo "the variable define at $APP_ENV"
